Run with `julia --project=.. example.jl filename` where filename is a txt `ofile containing a matrix

If you want to use RandESC in other directory, do (inside JULIA REPL)
```
import Pkg
Pkg.develop(path="/path/to/RandESC/)
```